server:
  port: ${HELPDESK_PORT:8081}

spring:
  datasource:
    url: jdbc:mysql://${HELPDESK_DB_HOST:127.0.0.1}:${HELPDESK_DB_PORT_MYSQL:3306}/${HELPDESK_DB_NAME:helpdesk_dev}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&useUnicode=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${HELPDESK_DB_USER:root}
    password: ${HELPDESK_DB_PASSWORD:12345678}
  mail:
    host: smtp.qq.com
    port: 465
    username: 2450695525@qq.com
    password: ndzawsddnkrtdibg
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          starttls:
            enable: false
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 50MB

app:
  upload-dir: ${HELPDESK_UPLOAD_DIR:../uploads}
  cors:
    allowed-origins: ${HELPDESK_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://116.128.189.228:8088,http://116.128.189.228}
  mail:
    enabled: true
    from: 2450695525@qq.com
    subject-prefix: "[Helpdesk]"

logging:
  level:
    org.hibernate.SQL: warn
